# Renewals Coordinator Legacy Template

Renewals are folded into `templates/leasing-coordinator` because renewals are part of the leasing lifecycle. Use `templates/leasing-coordinator` unless you intentionally split renewals into a standalone role.
